/**
 * Testing the `break` and `continue` statements.
 *
 * An infinite loop that skips printing "5", and stops at 11.
 */

let a = 0;
let b = 1;
while true {
    a = a + 1;
    if a == 5 continue;
    print "a=" + a + "\r\n";
    if a>10 break;
}

